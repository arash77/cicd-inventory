---
interface Props {
  src: string;
  alt?: string;
  workflowUrl?: string;
}

const { src, alt = 'Build status', workflowUrl } = Astro.props;
---

{workflowUrl ? (
  <a
    href={workflowUrl}
    target="_blank"
    rel="noopener noreferrer"
    aria-label={`${alt} – opens in new tab`}
    title={alt}
    class="shrink-0 inline-flex items-center cursor-pointer rounded overflow-hidden transition-opacity duration-150 hover:opacity-80"
  >
    <img src={src} alt={alt} loading="eager" class="h-5 w-auto max-w-[220px] block object-contain" />
  </a>
) : (
  <img src={src} alt={alt} loading="eager" class="h-5 w-auto max-w-[220px] block object-contain shrink-0" />
)}
